apiVersion: batch/v1
kind: CronJob
metadata:
  name: auto-sync-mini-store
  namespace: test
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: auto-sync-sa
          containers:
          - name: auto-sync
            image: bitnami/kubectl:latest
            command:
              - /bin/bash
              - -c
              - |
                apt update && apt install -y curl jq bash
                bash /scripts/check-and-update.sh
            volumeMounts:
              - name: scripts
                mountPath: /scripts
          restartPolicy: OnFailure
          volumes:
            - name: scripts
              configMap:
                name: auto-sync-script